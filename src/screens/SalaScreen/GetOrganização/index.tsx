import { authService } from "../../../services/auth/authService"

export default  function Organizacoes(ctx) {
    // const dadosOrganiza =  authService.organiza(ctx)
    // console.log(dadosOrganiza)

    return (
        <h1>Nome da organização</h1>
    )
}

